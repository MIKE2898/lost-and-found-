Lost and Found API

Project Description

An API for Reporting or Finding Lost Items

Contributors

* Lenora
* Mike
* Ludwig

How to Push Work to GitHub

Run the following commands from the project folder:

git init
git add .
git commit -m "What you did"
git branch -M main
git remote add origin <YOUR_GITHUB_REPOSITORY_URL>
git push -u origin main

Example

git init
git add .
git commit -m "Added user authentication"
git branch -M main
git remote add origin https://github.com/username/lost-and-found-api.git
git push -u origin main

For Future Updates

After making changes to the project:

git add .
git commit -m "Describe your changes"
git push
